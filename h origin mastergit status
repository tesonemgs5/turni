[1mdiff --git a/src/App.jsx b/src/App.jsx[m
[1mindex 8e26d52..6e0b58c 100644[m
[1m--- a/src/App.jsx[m
[1m+++ b/src/App.jsx[m
[36m@@ -1102,24 +1102,11 @@[m [mfunction sortedModelli(){[m
     const rot = rotazioni.find(r=>r.id===rotId);[m
     if(!rot) return;[m
 [m
[31m-    const modLav = modelli.find(m=>m.id===rot.modellaLavoroId);[m
[31m-    const modRip = modelli.find(m=>m.id===rot.modelloNLId);[m
[31m-    const totalWeeks = numRipetizioni * 4;[m
[31m-[m
[31m-    const [y0, m0, d0] = startDayKey.split("-").map(Number);[m
[31m-    const start = new Date(y0, m0-1, d0);[m
[31m-[m
     const nuoviEventiLocali = {};[m
 [m
[31m-    for(let i=0; i<totalWeeks; i++) {[m
[31m-      const d = new Date(start);[m
[31m-      d.setDate(d.getDate() + i * 7);[m
[31m-      const dateKey = dkey(d.getFullYear(), d.getMonth(), d.getDate());[m
[31m-[m
[31m-      const isLavoro = (i % 4) === 0;[m
[31m-      const mod = isLavoro ? modLav : modRip;[m
[31m-      if(!mod) continue;[m
[31m-[m
[32m+[m[32m    async function inserisciEvento(mod, dataEv){[m
[32m+[m[32m      if(!mod) return;[m
[32m+[m[32m      const dateKey = dkey(dataEv.getFullYear(), dataEv.getMonth(), dataEv.getDate());[m
       const color = mod.coloreCustom || (mod.tempo==="h24" ? "#64748b" : colByTime(mod.inizio));[m
       const label = (mod.label || mod.titolo || "").toUpperCase();[m
       const allDay = mod.tempo==="h24";[m
[36m@@ -1148,7 +1135,7 @@[m [mfunction sortedModelli(){[m
 [m
       if(error) {[m
         console.error("Errore inserimento evento rotazione:", error);[m
[31m-        continue;[m
[32m+[m[32m        return;[m
       }[m
 [m
       if(!nuoviEventiLocali[dateKey]) nuoviEventiLocali[dateKey] = {};[m
[36m@@ -1170,6 +1157,53 @@[m [mfunction sortedModelli(){[m
       });[m
     }[m
 [m
[32m+[m[32m    if(rot.tipo === "nlrs_scalante") {[m
[32m+[m[32m      // Ordine fisso: parte sempre dal primo modello (RS), poi NL a +7gg[m
[32m+[m[32m      const modRS = modelli.find(m=>m.id===rot.modelloRSId);[m
[32m+[m[32m      const modNL = modelli.find(m=>m.id===rot.modelloNLId);[m
[32m+[m[32m      const GIORNI_CICLO = [5, 4, 3, 2, 1, 6];[m
[32m+[m[32m      const [y0, m0, d0] = startDayKey.split("-").map(Number);[m
[32m+[m[32m      let dataCorrRS = new Date(y0, m0-1, d0);[m
[32m+[m[32m      let giornoCicloIdx = 0;[m
[32m+[m[32m      const totalCicli = numRipetizioni * GIORNI_CICLO.length;[m
[32m+[m
[32m+[m[32m      for(let i=0; i<totalCicli; i++) {[m
[32m+[m[32m        const dataRS = new Date(dataCorrRS);[m
[32m+[m[32m        const dataNL = new Date(dataCorrRS);[m
[32m+[m[32m        dataNL.setDate(dataNL.getDate() + 7);[m
[32m+[m
[32m+[m[32m        await inserisciEvento(modRS, dataRS);[m
[32m+[m[32m        await inserisciEvento(modNL, dataNL);[m
[32m+[m
[32m+[m[32m        giornoCicloIdx = (giornoCicloIdx + 1) % GIORNI_CICLO.length;[m
[32m+[m[32m        const prossimoDow = GIORNI_CICLO[giornoCicloIdx];[m
[32m+[m[32m        const base = new Date(dataCorrRS);[m
[32m+[m[32m        base.setDate(base.getDate() + 21);[m
[32m+[m[32m        let tentativo = new Date(base);[m
[32m+[m[32m        let iter = 0;[m
[32m+[m[32m        while(tentativo.getDay() !== prossimoDow && iter < 14) {[m
[32m+[m[32m          tentativo.setDate(tentativo.getDate() + 1);[m
[32m+[m[32m          iter++;[m
[32m+[m[32m        }[m
[32m+[m[32m        dataCorrRS = tentativo;[m
[32m+[m[32m      }[m
[32m+[m[32m    } else {[m
[32m+[m[32m      const modLav = modelli.find(m=>m.id===rot.modellaLavoroId);[m
[32m+[m[32m      const modRip = modelli.find(m=>m.id===rot.modelloNLId);[m
[32m+[m[32m      const totalWeeks = numRipetizioni * 4;[m
[32m+[m
[32m+[m[32m      const [y0, m0, d0] = startDayKey.split("-").map(Number);[m
[32m+[m[32m      const start = new Date(y0, m0-1, d0);[m
[32m+[m
[32m+[m[32m      for(let i=0; i<totalWeeks; i++) {[m
[32m+[m[32m        const d = new Date(start);[m
[32m+[m[32m        d.setDate(d.getDate() + i * 7);[m
[32m+[m[32m        const isLavoro = (i % 4) === 0;[m
[32m+[m[32m        const mod = isLavoro ? modLav : modRip;[m
[32m+[m[32m        await inserisciEvento(mod, d);[m
[32m+[m[32m      }[m
[32m+[m[32m    }[m
[32m+[m
     setStore(prev => {[m
       const ns = JSON.parse(JSON.stringify(prev));[m
       for(const [dateKey, calMap] of Object.entries(nuoviEventiLocali)) {[m
[36m@@ -3257,7 +3291,7 @@[m [mfunction sortedModelli(){[m
                     return ([m
                       <div key={r.id} style={{borderBottom:i<arr.length-1?`1px solid ${T.border}`:"none"}}>[m
                         <div onClick={()=>{[m
[31m-                          if(r.tipo==="domeniche"){[m
[32m+[m[32m                          if(r.tipo==="domeniche"||r.tipo==="nlrs_scalante"){[m
                             setShowApplyRotDialog(r);[m
                           } else {[m
                             setShowRotDetail(r);[m
[36m@@ -3474,9 +3508,12 @@[m [mfunction sortedModelli(){[m
             onClick={e=>e.stopPropagation()}>[m
             <div style={{fontSize:16,fontWeight:900,color:T.text,marginBottom:8}}>Applica Rotazione</div>[m
             <div style={{fontSize:13,color:T.sub,marginBottom:16}}>[m
[31m-              Stai applicando la rotazione <strong>{showApplyRotDialog.titolo||"Senza nome"}</strong> a partire da domenica {dayKey}.[m
[32m+[m[32m              Stai applicando la rotazione <strong>{showApplyRotDialog.titolo||"Senza nome"}</strong> a partire dal {dayKey}[m
[32m+[m[32m              {showApplyRotDialog.tipo==="nlrs_scalante"?" (primo RS)":" (domenica)"}.[m
               <br/><br/>[m
[31m-              Il ciclo è di 4 domeniche. Quante volte vuoi ripeterlo?[m
[32m+[m[32m              {showApplyRotDialog.tipo==="nlrs_scalante"[m
[32m+[m[32m                ?"Il ciclo scalante è di 6 coppie RS/NL. Quante volte vuoi ripeterlo?"[m
[32m+[m[32m                :"Il ciclo è di 4 domeniche. Quante volte vuoi ripeterlo?"}[m
             </div>[m
 [m
             <div style={{display:"flex",alignItems:"center",gap:10,marginBottom:20}}>[m
